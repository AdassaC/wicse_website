
       /*
        This is a manifest file that'll be compiled into application.css.erb, which will include all the files
        * listed below.
        *
        * Any CSS and SCSS file within this directory, lib/assets/stylesheets, or any plugin's
        * vendor/assets/stylesheets directory can be referenced here using a relative path.
        *
        * You're free to add application-wide styles to this file and they'll appear at the bottom of the
        * compiled file so the styles you add here take precedence over styles defined in any other CSS/SCSS
        * files in this directory. Styles in this file should be added after the last require_* statement.
        * It is generally better to create a new file per style scope.
        */
 /*

    body {
        /*font-family: system-ui;
        background:#c5defa;
        /*color: #8fb58d;
        text-align: center;
        }
    h1 {
        font-family: system-ui;
        color: #8fb58d;
        text-align: center;
    }
    h2 {
        font-family: "Robto M";
        color: #8fb58d;
        text-align: center;
    }

    h3 {
        font-family: Futura;
        color: #8fb58d;
        text-align: left;
    }

    p {
        font-family: optima;
        color: #4d644b;
        text-align: left;
    }
*/
   ::after,
   ::before {
       box-sizing: border-box;
   }

/* fonts */

@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;600&family=Montserrat&display=swap');
html {
    font-size: 100%;
} /*16px*/

:root {
    /* colors */
    --primary-100: #f3f8ff;
    --primary-200: #9bcbff;
    --primary-300: #80b7ff;
    --primary-400: #3c92f5;
    --primary-500: #00488d;
    --primary-600: #246fb7;
    --primary-700: #01488f;
    --primary-800: #023362;
    --primary-900: #01203f;

    /* grey */
    --grey-50: #f8fafc;
    --grey-100: #f1f5f9;
    --grey-200: #e2e8f0;
    --grey-300: #cbd5e1;
    --grey-400: #94a3b8;
    --grey-500: #64748b;
    --grey-600: #475569;
    --grey-700: #334155;
    --grey-800: #1e293b;
    --grey-900: #ffffff;
    /* rest of the colors */
    --black: #222;
    --white: #fff;
    --red-light: #f8d7da;
    --red-dark: #842029;
    --green-light: #d1e7dd;
    --green-dark: #0f5132;

    /* fonts  */
    --headingFont: 'Roboto', sans-serif;
    --bodyFont: 'optima', sans-serif;
    --smallText: 0.7em;
    /* rest of the vars */
    --backgroundColor: #edf5ff;
        /*var(--grey-50)*/
    --textColor: var(--grey-900);
    --borderRadius: 0.25rem;
    --letterSpacing: 1px;
    --transition: 0.3s ease-in-out all;
    --max-width: 1120px;
    --fixed-width: 600px;

    /* box shadow*/
    --shadow-1: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
    --shadow-2: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
    0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --shadow-3: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
    0 4px 6px -2px rgba(0, 0, 0, 0.05);
    --shadow-4: 0 20px 25px -5px rgba(0, 0, 0, 0.1),
    0 10px 10px -5px rgba(0, 0, 0, 0.04);
}

body {
    background: var(--backgroundColor);
    font-family: var(--bodyFont);
    font-weight: 400;
    line-height: 1.75;
    color: var(--textColor);
}

p {
    margin-top: 0;
    margin-bottom: 1.5rem;
    max-width: 40em;
}

h1,
h2,
h3,
h4,
h5 {
    margin: 0;
    margin-bottom: 1.38rem;
    font-family: var(--headingFont);
    font-weight: 400;
    line-height: 1.3;
    text-transform: capitalize;
    letter-spacing: var(--letterSpacing);
}

h1 {
    margin-top: 0;
    font-size: 2rem;
}

h2 {
    font-size: 1.75rem;
}

h3 {
    font-size: 1.25rem;
}

h4 {
    font-size: 1rem;
}

h5 {
    font-size: .6rem;
}

small,
.text-small {
    font-size: var(--smallText);
}

a {
    text-decoration: none;
}
ul {
    list-style-type: none;
    padding: 0;
}

.img {
    width: 100%;
    display: block;
    object-fit: cover;
}
/* buttons */

.btn {
    cursor: pointer;
    color: var(--white);
    background: var(--primary-500);
    border: transparent;
    border-radius: var(--borderRadius);
    letter-spacing: var(--letterSpacing);
    padding: 0.275rem 0.75rem;
    box-shadow: var(--shadow-1);
    transition: var(--transition);
    text-transform: capitalize;
    display: inline-block;
}
.btn:hover {
    background: var(--primary-700);
    box-shadow: var(--shadow-3);
}
.btn-hipster {
    color: var(--primary-500);
    background: var(--primary-200);
}
.btn-hipster:hover {
    color: var(--primary-200);
    background: var(--primary-700);
}
.btn-block {
    cursor: pointer;
    color: var(--white);
    background: var(--primary-500);
    border: transparent;
    border-radius: var(--borderRadius);
    letter-spacing: var(--letterSpacing);
    padding: 0.275rem 0.75rem;
    box-shadow: var(--shadow-1);
    transition: var(--transition);
    text-transform: capitalize;
    display: inline-block;
    max-width: 100%;
    margin-bottom: 1rem;
}

/* alerts */
.alert {
    padding: 0.375rem 0.75rem;
    margin-bottom: 1rem;
    border-color: transparent;
    border-radius: var(--borderRadius);
}

.alert-danger {
    color: var(--red-dark);
    background: var(--red-light);
}
.alert-success {
    color: var(--green-dark);
    background: var(--green-light);
}
/* form */

.form {
    color: var(--grey-800);
    width: 90vw;
    max-width: var(--fixed-width);
    background: var(--white);
    border-radius: var(--borderRadius);
    box-shadow: var(--shadow-2);
    padding: 2rem 2.5rem;
    margin: 3rem auto;
}
.form-label {
    display: block;
    font-size: var(--smallText);
    margin-bottom: 1rem;
    text-transform: capitalize;
    letter-spacing: var(--letterSpacing);

}
.form-input,
.form-textarea {
    width: 96%;
    padding: 0.375rem 0.75rem;
    border-radius: var(--borderRadius);
    background: var(--backgroundColor);
    border: 1px solid var(--grey-200);
}

.form-row {
    margin-bottom: 0rem;
}

.form-textarea {
    height: 7rem;
}
::placeholder {
    font-family: inherit;
    color: var(--grey-400);
}
.form-alert {
    color: var(--red-dark);
    letter-spacing: var(--letterSpacing);
    text-transform: capitalize;
}
/* alert */

@keyframes spinner {
    to {
        transform: rotate(360deg);
    }
}

.loading {
    width: 6rem;
    height: 6rem;
    border: 5px solid var(--grey-400);
    border-radius: 50%;
    border-top-color: var(--primary-500);
    animation: spinner 0.6s linear infinite;
}
.loading {
    margin: 0 auto;
}
/* title */

.title {
    text-align: center;
}

.title-underline {
    background: var(--primary-500);
    width: 7rem;
    height: 0.25rem;
    margin: 0 auto;
    margin-top: -1rem;
}

/*NAVIGATION BAR*/
.navbar{
    display: flex;
    justify-content: center;
    align-items: center;
}
.nav-center{
    width: 90vw;
    max-width:var(--max-width);
}

.nav-header{
    height: 6rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.nav-header img{
    width: 215px;
}

.nav-logo{
   display: flex;
   align-items: center;
   justify-content: center;
    height: auto;
}

.nav-btn{
    padding: .15rem 0.75rem;
}
.nav-btn i{
    font-size: 1.25rem;
}
.nav-links{
    width: auto;
    padding: 0.15rem .75rem;
    height: 0;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: var(--transition);
}

.show-links{
    height: 310px;
}

.nav-link{
    display: block;
    text-align: center;
    text-transform: capitalize;
    color: var(--grey-900);
    letter-spacing: var(--letterSpacing);
    padding: 1rem 0;
    border-top: 1px solid var(--grey-500);
    transition: var(--transition);
}
.nav-link:hover{
    color: var(--primary-600);
}
.contact-link a {
    padding: 0.15rem 1rem;
}

@media screen and (min-width: 992px) {
    .navbar{
        height: 6rem;
    }

    .nav-center{
        display: flex;
        align-items: center;

    }
    .nav-header{
        padding: 0;
        height: auto;
        margin-right: 2rem;
    }
    .nav-btn {
        display: none;
    }
    .nav-links {
        height: auto !important;
        flex-direction: row;
        align-items: center;
        /*width: 100%;*/
    }
    .nav-link {
        padding: 0;
        border-top: none;
        margin-right: 1rem;
        font-size: 1rem;
    }
    .contact-link{
        margin-right: 0;
        margin-left: auto;
    }
}
/*PAGE*/
.page {
    width: 90vw;
    max-width: var(--max-width);
    margin: 0 auto;
}
.page {
   padding-top: 2rem;
   min-height: calc(100vh - (10rem));
}

.basic_container{
    padding-top: 1rem;
    padding-bottom: 1rem;
    background: white;
    position: center;
    border-radius: 15px;
    box-shadow: var(--shadow-2);
}

/*FOOTER*/
.page-footer{
    height: 4rem;
    background: var(--black);
    color:var(--white);
    display: flex;
    align-items: center;
    justify-content: center;
}
.page-footer p{
    margin-bottom: 0;
}
.footer-logo,
.page-footer a{
    color: var(--primary-500);
}

/*HERO*/
.hero{
   height: 70vh;
    position: relative;
   margin-bottom: 2rem;
    border-radius: var(--borderRadius);
    background: url(https://images.pexels.com/photos/6146970/pexels-photo-6146970.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260) center/cover no-repeat;
}

.hero-container{
   position: absolute;
   top: 0;
   left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
   background: rgba(0,0,0,0.4);
   border-radius: var(--borderRadius);
}
.hero-text{
   color: var(--white);
    text-align: center;
}
@media screen and (min-width: 768px) {
   .hero-text h1{
       font-size: 4rem;
       margin-bottom: 0;
   }
}

/*ABOUT*/
.about-page h2{
    font-weight: bolder;
    width: 97%;
    color: var(--grey-800);
    text-align: right;
    margin-bottom: 1rem;
    text-transform: revert;
}
.about-page h4{
    color: var(--grey-800);
    font-weight: bold;
    margin-bottom: 1rem;
    margin-left: 2rem;
    margin-right: 2rem;
}
.about_container{
    height: 16rem;
}
.about-page p{
    text-transform: none;
    color: var(--grey-800);
    margin-bottom: 2rem;
    margin-left: 2rem;
    margin-right: 2rem;
}
.about-page {
    align-items: center;
    justify-content: center;
    display: grid;
    gap: 2rem 4rem;
}
@media screen and (min-width: 992px){
    .about-page {
        align-items: center;
        justify-items: center;
    }

}
/*SIGN UP*/
.sign_up {

}
.sign_up-form {
    width: 100%;
}
.btn-block_login{
   width: 100%;
    padding: 0.275rem 0.75rem;
}
@media screen and (min-width: 992px) {
    .sign_up-form {
        align-items: center;
        justify-items: center;
    }
    .btn-block_login{
        width: 100%;
        padding: 0.275rem 0.75rem;
    }
}
       /*LOGIN*/

       /*USER MAIN*/
       .logout-link a{
           padding: 0.15rem 1rem;
       }
       @media screen and (min-width: 992px) {
           .logout-link {
               margin-right: 0;
               margin-left: auto;
           }
       }

       /*COLLEGES INDEX*/
       .add-college_btn {
           position: relative;
           left: 44%;
           margin-bottom: 2rem;
       }
       .search_area{
           width: 79.9%
       }
       .school_table_container{
           padding-top: 2rem;
           min-height: calc(100vh - (10rem));
           background: white;
           position: center;
           width: 100%;
           height: 100%;
           border-radius: 15px;
           box-shadow: var(--shadow-2);
       }
       .school_table{
           width: 100%;
           position: relative;
       }
       .school_table_header{
           display: revert;
           color: #2E2F30;
           font-family: Futura;
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           justify-content: center;
           text-align: center;
           margin-top: 3rem;
       }
       .school_table_header th {
           height: 35px;
           display: table-cell;
           vertical-align: inherit;
           text-align: center;
           font-weight: bold;
           justify-self: stretch;
       }
       .school_table_data{
           color: #2E2F30;
           background: white;
           font-family: var(--headingFont);
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           width: 100%;
           text-align: center;
       }
       .school_table_data td {
           display: table-cell;
           justify-self: stretch;
           max-width: 20px;
       }
       @media screen and (min-width: 992px) {
           .find_school-page{
           }
           .search_btn{
               width: 15%;
               padding: 0.275rem 0.75rem;
               height: 1.89rem;
           }
       }

       /*WRITE REVIEW*/
       .review-page h2 {
           width: 97%;
           color: #00488d;
           /*font-weight: bold;*/
           position: relative;
           margin: 1rem;
           right: -16rem;

       }
       .review-page{
           align-items: center;
           justify-content: center;
           display: grid;
           gap: 2rem 4rem;
       }
       .review_container {
           width: 100%;
           height: fit-content;
           padding: 2rem;
       }
       .write_review {
           margin-top: 0;
           box-shadow: none;

       }
       .course_name_code{
           box-shadow: none;
           width: auto;
           margin-outside: 0;
           height: fit-content;
           margin-top: 0;
           margin-bottom: 0;
           justify-content: left;
           color: #00488d;

       }
       .course_name_input{
           width: 30%;
           justify-items: left;
           margin-right: 15.22rem;
           margin-left: 0rem;
           color: #00488d;
       }
       .course_code_input{
           width: 20%;
           justify-self: right;
           margin-right: 0rem;
           margin-left: 0rem;
           color: #00488d;
       }

       .y_t_prof{
           box-shadow: none;
       }
       .course_year_input {

       }
       .course_term_input{

       }
       .course_prof_input{

       }
       .ratings {
           color: #00488d;
       }
       .review_text_area{
           box-shadow: none;
       }
       .review_text_area_header{
           height: fit-content;
           justify-items: center;
           padding-bottom: .05rem;
           padding-top: .05rem;
           margin-bottom: 1rem;
           background: rgb(1, 81, 151);
       }
       .review_text_area_header h3{
           text-align: center;
           margin-bottom: .5rem;
           margin-top: .5rem;
           color: white;
       }
       .course_review_input{
           margin-top: .05rem;
           height: 10rem;
           margin-bottom: 1rem;
           color: #00488d;
       }
       .review_submit_btn {
           width: 100%;
           margin-top: 1rem;
       }
       @media screen and (min-width: 992px) {
           .review-page {
               align-items: center;
               justify-items: center;
           }
       }

      /*COLLEGES_SHOW*/
       .colleges-page {
           color: var(--primary-500);
       }
       .colleges-page h2{
           font-family: "Arial Rounded MT Bold";
           font-weight: bolder;
           margin-bottom: 1.3rem;
           margin-left: .5rem;
       }
       .colleges-page h4 {
           color: #2E2F30;
           margin-bottom: 2rem;
           margin-left: 2rem;
           margin-right: 2rem;
       }
       .courses_in_colleges_container {
           padding-top: 2rem;
           min-height: calc(100vh - (10rem));
           background: white;
           position: center;
           width: 100%;
           height: fit-content;
           border-radius: 15px;
           box-shadow: var(--shadow-2);
       }
       .courses_at_college_table {
           width: 100% ;
           position: relative;
       }
       /*.colleges-page tbody {
           width: 100%;
           position: relative;
           margin: 2rem;
           align-self: center;
       }*/
       .college_courses_header {
           color: #2E2F30;
           width: 100%;
           display: revert;
           font-family: Futura;
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           justify-content: center;
           text-align: center;
       }
       .college_courses_header th {
           display: table-cell;
           vertical-align: inherit;
           text-align: center;
           font-weight: bold;
           justify-self: stretch;
       }
       /*.colleges-page {
           width: fit-content;
           margin-left: 2rem;
           margin-right: 2rem;
           margin-bottom: 2rem;
       }*/
       .course_at_college {
           width: 100%;
           font-family: var(--headingFont);
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           text-align: center;
       }
       @media screen and (min-width: 992px) {
           .colleges-page {
               align-items: center;
               justify-items: center;
           }
       }

       /*COLLEGES_NEW*/
       .college_new-page {
           align-items: center;
           justify-content: center;
           display: grid;
           gap: 2rem 4rem;
       }
       .college_new_page_h2 {
           width: 97%;
           color: #00488d;
           /*font-weight: bold;*/
           text-align: center;
           margin: 1rem;
           right: -16rem;
       }

       /*COURSES_NEW*/
       .course_new-page {
           align-items: center;
           justify-content: center;
           display: grid;
           gap: 2rem 4rem;
       }
       .course_new_page_h2 {
           width: 97%;
           color: #00488d;
           /*font-weight: bold;*/
           text-align: center;
           margin: 1rem;
           right: -16rem;
       }

       /*COURSES_SHOW*/
       .course_show-page {
           color: var(--primary-500);
       }
       .course_show-page h2 {
           font-family: "Arial Rounded MT Bold";
           font-weight: bolder;
           margin-bottom: 1.3rem;
       }
       .course_reviews_header {
           margin-bottom: 3rem;
       }
       .course_show-page h4 {
           color: #2E2F30;
           margin-bottom: 2rem;
           margin-left: 2rem;
           margin-right: 2rem;
       }
       .add-review_btn {
           position: fixed;
           margin-left: 30%;
           margin-top: -3%;
       }
       .reviews_in_course_container {
           padding-top: 1rem;
           padding-bottom: 1rem;
           background: white;
           size: auto;
           border-radius: 15px;
           box-shadow: var(--shadow-2);
           height: fit-content;
           width: 50%;
           min-width: 312px;

       }
       .reviews_container {
           position: center;
           border-radius: 15px;
           box-shadow: var(--shadow-2);
           margin: 2rem;
           background-color: #eaf0f7;
           height: fit-content;
           width: fit-content;
           padding: 1rem;
       }
       .review_rating_icon {
           border-radius: 100%;
           background: #246fb7;
       }
       .review_course_info {
           color: #2E2F30;
           font-weight: bold;
           font-family: Futura;
           margin-left: 80%;
           display: inline-flex;

       }
       .review_summary_display {
           color: #2E2F30;
       }
       .reviewer_info {
           text-align: right;
       }
       @media screen and (min-width: 992px) {
           .course_show-page h2 {
               justify-self: center;
           }
           .reviews_in_course_container {
               justify-self: center;
               width: 100%;
           }
           .course_show-page {
               align-items: center;
               justify-items: center;
           }
           .reviews_container {
               position: center;
               border-radius: 15px;
               box-shadow: var(--shadow-2);
               margin: 2rem;
               width: 91.5%;
               background-color: #eaf0f7;
               height:fit-content;
               padding: 1rem;
           }
           .add-review_btn {
               margin-left: 58%;
               margin-top: -2.5%;
           }
       }


       /*COURSE INDEX*/
       .add-course_btn {
           position: relative;
           left: 44%;
           margin-bottom: 2rem;
       }
       .courses_table {
           width: 100%;
           position: relative;
       }
       .courses_table_container {
           padding-top: 2rem;
           min-height: calc(100vh - (10rem));
           background: white;
           position: center;
           width: 100%;
           height: fit-content;
           border-radius: 15px;
           box-shadow: var(--shadow-2);
       }
       .courses_table_header{
           display: revert;
           color: #2E2F30;
           font-family: Futura;
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           justify-content: center;
           text-align: center;
           margin-top: 3rem;
       }
       .courses_table_header th {
           height: 35px;
           display: table-cell;
           vertical-align: inherit;
           text-align: center;
           font-weight: bold;
           justify-self: stretch;
       }
       .courses_table td {
           margin-top: 1rem;
           display: table-cell;
           justify-self: stretch;
           height: auto;
           color: #2E2F30;
           vertical-align: inherit;
       }
       .courses_table_data {
           margin-right: 1rem;
           margin-left: 1rem;
           color: #2E2F30;
           background: white;
           font-family: var(--headingFont);
           font-style: normal;
           font-weight: normal;
           line-height: 1.3;
           text-transform: capitalize;
           letter-spacing: var(--letterSpacing);
           width: 100%;
           text-align: center;
       }


